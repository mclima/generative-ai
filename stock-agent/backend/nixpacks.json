{
  "phases": {
    "setup": {
      "nixPkgs": ["python311", "pip"]
    },
    "install": {
      "cmds": ["pip install -r requirements.txt"]
    },
    "build": {
      "cmds": ["python download_models.py"]
    }
  },
  "start": {
    "cmd": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
